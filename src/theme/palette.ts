import { createTheme } from "@mui/system";

export const customTheme = createTheme({
  palette: {
    primary: {
      main: "#1b6ab3",
      dark: "#052440",
    },
    secondary: {
      main: "#000",
    },
    neutral: {
      black: "#000",
      white: "#fff",
      red: "#F83B00",
      grey: "#b8b6b6",
    },
  },
});
